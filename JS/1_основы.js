// Задание: Нужно написать функцию, которая возвращает тип входного аргумента на входе: один аргумент, любого значения; на выходе: строка, с типом входного аргумента
function getType(inp) {
  return typeof inp;
}

console.log(getType(530));
console.log(getType("da"));